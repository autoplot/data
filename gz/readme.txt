Autoplot should automatically download and unzip .gz files.  That is, if "x.cdf" is requested, "x.cdf.gz" can be downloaded and unzipped.

Note this currently does not work with mediawiki, because it doesn't return a 404 code.
